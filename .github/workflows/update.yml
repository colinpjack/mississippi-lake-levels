name: Update lake briefing

on:
  schedule:
    # GitHub cron is UTC and often 5–60+ minutes late on free plans.
    # Target America/Toronto 8:00 / 13:00 / 18:00 during EDT (UTC-4).
    - cron: "0 12 * * *" # ~8:00 AM EDT
    - cron: "0 17 * * *" # ~1:00 PM EDT
    - cron: "0 22 * * *" # ~6:00 PM EDT
  workflow_dispatch: # manual "Run workflow" button

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  update-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Refresh gauges, chart, and index.html
        run: python update_site.py

      - name: Commit updated site (if changed)
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html chart.png data/chart_series.json
          if git diff --staged --quiet; then
            echo "No content changes"
          else
            git commit -m "chore: refresh Mississippi Lake briefing $(date -u +%Y-%m-%dT%H:%MZ)"
            git push
          fi

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: .

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
